import React from "react";

const page = () => {
  
    return (
    <div>
      <h2>learn kafka</h2>
      <p>
        Kafka is used to send, receive, and process large amounts of data in
        real time between systems.
      </p>
    </div>
  );
};

export default page;
